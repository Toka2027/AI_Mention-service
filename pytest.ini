[pytest]
# Tests live in service_engine/ and import the `engine` package. Adding
# service_engine to the path lets `python -m pytest` run from the repo ROOT
# (previously it errored on `import engine`). Running from service_engine/ also works.
pythonpath = service_engine
testpaths = service_engine/tests
addopts = -q
